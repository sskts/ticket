import { Component, Inject, forwardRef } from '@angular/core';

@Component({
    selector: 'header',
    template: `
        <div class="header">
        </div>
    `
})

export class headerComponent { 

    
}

