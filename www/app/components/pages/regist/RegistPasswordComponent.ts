import { Component, Inject, forwardRef } from '@angular/core';
import { PageComponent } from '../../../components/pages/PageComponent';

@Component({
    selector: 'regist-password',
    template: `
        regist-Password
    `
})
export class RegistPasswordComponent {

    
}

